import { DynamicComponentsLoadDirective } from './dynamic-components-load.directive';

describe('DynamicComponentsLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicComponentsLoadDirective();
    expect(directive).toBeTruthy();
  });
});
